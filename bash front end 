# Run the backend container
docker run -d -p 5000:5000 --name threat-backend threat-detection-backend

# Run the frontend container
docker run -d -p 80:80 --name threat-frontend threat-detection-frontend